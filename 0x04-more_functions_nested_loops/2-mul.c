#include "main.h"
/**
 * mul - it multiplies two integers
 * @a: integers to be multiplied with b
 * @b: integers to be multiplied with a
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
