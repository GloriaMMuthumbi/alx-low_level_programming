#include "main.h"
/**
 * _abs - this function calculates the absolute
 * value of an integer
 *
 * @n: the integer to be checked
 *
 * Return: returhns the absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
