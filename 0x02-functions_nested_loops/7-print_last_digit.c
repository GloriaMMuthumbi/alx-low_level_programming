#include "main.h"
/**
 * print_last_digit - it prints the last digit of n
 *
 * @n: the int that the last digit is gotten from
 *
 * Return: returns value of the last digit of n
 */

int print_last_digit(int n)
{
	int d;

	if (n < 0)
		n = -n;
	d = n % 10;

	if (d < 0)
		d = -d;
	
	_putchar(a+'0');

	return (a);
}
