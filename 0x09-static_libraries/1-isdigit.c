#include "main.h"
/**
 * _isdigit - checks if integer is a digit
 * @c: the integer to be checked
 *
 * Return: returns 1 if integer is a digit
 * and 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
