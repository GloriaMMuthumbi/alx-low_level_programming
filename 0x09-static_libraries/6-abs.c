#include "main.h"
/**
 * _abs - returns the absolute valuse of an integer
 * @n: the integer to be used
 *
 * Return: returns the aboslute value of c
 */
int _abs(int n)
{
	return (n < 0 ? -n : n);
}
