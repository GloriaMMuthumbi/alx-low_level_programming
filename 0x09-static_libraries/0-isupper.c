#include "main.h"
/**
 * _isupper - checks if integer represents an
 * uppercase letter
 * @c: the integer to be cheacker
 *
 * Return: returns 1 if c is uppercase and 0
 * otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
